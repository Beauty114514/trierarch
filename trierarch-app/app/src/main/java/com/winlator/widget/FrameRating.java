package com.winlator.widget;

/** Minimal enum holder so Container metadata matches upstream shape. */
public final class FrameRating {
    public enum Mode { DISABLED, SIMPLE, FULL }

    private FrameRating() {}
}

